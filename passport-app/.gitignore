node_modules/
dist/
out/
*.log
.DS_Store
coverage/
userData/

*.swp
*.swo
*~
.idea/
.vscode/local

# local secrets — the app reads .env at runtime, CI writes its own from GitHub Secrets
.env
# publishing token for scripts/publish-release.mjs — never bundled into the installer
.env.release
